#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int i,j,n;
    while(scanf("%d",&n)==1)
    {
        for(i=0;i<n;i++)
        {
            for(i=0;i<40;i++)
            {
                scanf("%d",&a[i]);
                if(a[i]=0)
                    break;
            }

        }
    }
}
